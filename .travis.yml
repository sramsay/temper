
languge: cpp

dist: bionic
group: travis_latest

os: linux

matrix:
  include:
    - env: CC=gcc
    - env: CC=clang

install:
  - pip install meson ninja

script:
  - meson builddir
  - ninja -C builddir


