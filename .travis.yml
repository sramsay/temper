
languge: cpp

dist: eoan
sudo: required
group: travis_latest

os: linux

matrix:
  include:
    - env: CC=gcc
    - env: CC=clang

install:
  - pip3.6 install meson ninja

script:
  - meson builddir
  - ninja -C builddir


