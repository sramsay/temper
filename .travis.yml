
languge: cpp

dist: eoan
sudo: required
group: travis_latest

os: linux
language: python

matrix:
  include:
    - env: CC=clang

install:
	- pip install meson ninja

script:
  - meson build
  - ninja -C build


