dist: eoan
group: travis_latest

os: linux
language: python

matrix:
  include:
    - env: CC=g++
    - env: CC=clang++

install:
  - pip install meson ninja

script:
  - meson builddir
  - ninja -C build
  - ninja -C build

