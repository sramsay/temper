dist: bionic
group: travis_latest

os: linux
language: python

matrix:
  include:
    - os: linux
      compiler: gcc
      env: COMPILER=g++
      addons:
        apt:
          update: true
          packages:
            - libboost-all-dev
    - os: linux
      compiler: clang
      env: COMPILER=clang++
      addons:
        apt:
          update: true
          packages:
            - libboost-all-dev

install:
  - pip install meson ninja

script:
  - CXX=${COMPILER} meson --reconfigure build
  - ninja -v -C build
