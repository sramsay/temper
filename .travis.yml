
languge: cpp

dist: eoan
sudo: required
group: travis_latest

os: linux
language: python

install:
	- pip install meson ninja

matrix:
  include:
    - env: CC=gcc
    - env: CC=clang

script:
  - meson builddir
  - ninja -C builddir


