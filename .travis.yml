dist: eoan
group: travis_latest

os: linux
language: python

matrix:
  include:
    - env: CXX=g++
    - env: CXX=clang++

install:
  - pip install meson ninja

script:
  - meson build
  - ninja -C build
  - ninja -C build
