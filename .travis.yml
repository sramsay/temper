
languge: cpp

dist: bionic
sudo: required
group: travis_latest

os: linux

matrix:
  include:
    - env: CC=gcc
    - env: CC=clang

install:
  - pip3.8 install meson ninja

script:
  - meson builddir
  - ninja -C builddir


