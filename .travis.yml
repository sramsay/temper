dist: bionic
group: travis_latest
version: ~> 1.0

os: linux

matrix:
  include:
    - os: linux
      compiler: gcc
      env: COMPILER=gcc
    - os: linux
      compiler: clang
      env: COMPILER=clang

script:
  - CC=${COMPILER} ./configure
  - make
  - CC=${COMPILER} ./configure
  - make
